import 'package:flutter/material.dart';

class recamara extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('recamara'),
      ),
      body: Center(
        child: Text("Welcome to the recamara Page!"),
      ),
    );
  }
}
