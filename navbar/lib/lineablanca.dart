import 'package:flutter/material.dart';

class lineablanca extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('lineablanca'),
      ),
      body: Center(
        child: Text("Welcome to the lineablanca Page!"),
      ),
    );
  }
}
