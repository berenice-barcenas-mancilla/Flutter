import 'package:flutter/material.dart';

class sala extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('sala'),
      ),
      body: Center(
        child: Text("Welcome to the sala Page!"),
      ),
    );
  }
}
